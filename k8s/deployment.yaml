apiVersion: apps/v1
kind: Deployment
metadata:
  name: alethic-ism-processor-anthropic-deployment
  namespace: alethic
spec:
  replicas: 1 
  selector:
    matchLabels:
      app: alethic-ism-processor-anthropic
  template:
    metadata:
      labels:
        app: alethic-ism-processor-anthropic
    spec:
      volumes:
        - name: alethic-ism-processor-anthropic-secret-config-volume
          secret:
            secretName: alethic-ism-processor-anthropic-secret
            items:
              - key: .routing.yaml
                path: .routing.yaml
      containers:
      - name: alethic-ism-processor-anthropic
        image: <IMAGE>
        imagePullPolicy: Always
        volumeMounts:
          - name: alethic-ism-processor-anthropic-secret-config-volume
            mountPath: /app/repo/.routing.yaml
            subPath: .routing.yaml
            readOnly: true
        env:
          # Consumer Configuration for Pulsar
          - name: MSG_URL
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-anthropic-secret
                key: MSG_URL

          - name: MSG_TOPIC
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-anthropic-secret
                key: MSG_TOPIC

          - name: MSG_MANAGE_TOPIC
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-anthropic-secret
                key: MSG_MANAGE_TOPIC

          - name: MSG_TOPIC_SUBSCRIPTION
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-anthropic-secret
                key: MSG_TOPIC_SUBSCRIPTION

          # Database Storage Class requirement
          - name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-anthropic-secret
                key: DATABASE_URL

          # Primary Anthropic key
          - name: ANTHROPIC_API_KEY
            valueFrom:
              secretKeyRef:
                name: alethic-ism-processor-anthropic-secret
                key: ANTHROPIC_API_KEY
      imagePullSecrets:
      - name: regcred
